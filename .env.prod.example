# Production environment variables for Vercel

# Your Convex deployment URL (required)
PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Production deploy key from Convex Dashboard (required for automatic deployments)
# Format: prod:deployment-name|key
# Get from: Convex Dashboard → Settings → Deploy Keys → Generate Production Deploy Key
CONVEX_DEPLOY_KEY=prod:your-deployment|your-deploy-key

# Tolgee Production Configuration
# NOTE: Do NOT use API key in production. Use static translations or Tolgee CDN instead.
# For static exports: bunx tolgee pull (requires Tolgee CLI setup)
# Leave VITE_TOLGEE_API_KEY empty for production
VITE_TOLGEE_API_URL=https://app.tolgee.io
# VITE_TOLGEE_API_KEY= (intentionally empty for production)