{
	"$schema": "https://docs.renovatebot.com/renovate-schema.json",
	"extends": ["config:best-practices"],
	"schedule": ["before 6am on Monday"],
	"lockFileMaintenance": {
		"enabled": false
	},
	"osvVulnerabilityAlerts": true,
	"dependencyDashboardOSVVulnerabilitySummary": "all",
	"dependencyDashboardHeader": "This issue lists Renovate updates and detected dependencies. Read the [Dependency Dashboard](https://docs.renovatebot.com/key-concepts/dashboard/) docs to learn more.\n\nAn advisory listed without a pull request has no fixed release Renovate can reach, and this list carries no severity for it. Open its OSV record before ranking it against the others.",
	"vulnerabilityAlerts": {
		"description": "GitHub-Advisories ergänzen OSV unabhängig vom regulären Update-Zeitplan. Dafür müssen Dependabot-Alerts im Repository aktiviert sein. Der zweite Feed garantiert keine Abdeckung der OSV-Lücken: Ein Paket im Dependency Graph und ein passender Advisory-Versionsbereich reichen nicht als Nachweis, dass GitHub tatsächlich einen Alert erzeugt.",
		"enabled": true,
		"labels": ["security"],
		"schedule": ["at any time"]
	},
	"packageRules": [
		{
			"description": "Direkte Dependencies und Overrides werden gepinnt, weil Renovates Bun-Manager die tatsächlich installierten Versionen aus bun.lock nicht ausliest. Ein Versionsbereich kann eine behobene Version erlauben, während das Lockfile noch eine verwundbare enthält. Pinning hilft nur bei im Manifest deklarierten Paketen. Ausschließlich transitive Lockfile-Einträge fehlen weiterhin. Zusätzlich können unterschiedliche purl-Schreibweisen für Scoped-Pakete den OSV-Abgleich verhindern. Der GitHub-Feed bleibt als weitere Quelle aktiv, seine Abdeckung dieser Fälle ist jedoch nicht nachgewiesen. bun audit liest das Lockfile und ergänzt die Prüfung transitiver Pakete.",
			"matchDepTypes": ["dependencies", "overrides"],
			"rangeStrategy": "pin"
		},
		{
			"description": "Group all safe non-major updates together",
			"matchUpdateTypes": ["minor", "patch", "pin", "digest"],
			"groupName": "all non-major dependencies",
			"groupSlug": "all-minor-patch",
			"matchPackageNames": [
				"!better-auth",
				"!ai",
				"!eslint",
				"!@eslint/compat",
				"!@eslint/js",
				"!@convex-dev/agent",
				"!@convex-dev/better-auth",
				"!@mmailaender/convex-better-auth-svelte",
				"!@openrouter/ai-sdk-provider",
				"!/^@better-auth//",
				"!/^@ai-sdk//"
			]
		},
		{
			"description": "Group all safe major updates together",
			"matchUpdateTypes": ["major"],
			"groupName": "all major dependencies",
			"groupSlug": "all-major",
			"matchPackageNames": [
				"!better-auth",
				"!ai",
				"!autumn-js",
				"!typescript",
				"!eslint",
				"!@eslint/compat",
				"!@eslint/js",
				"!@convex-dev/agent",
				"!@convex-dev/better-auth",
				"!@mmailaender/convex-better-auth-svelte",
				"!@openrouter/ai-sdk-provider",
				"!/^@better-auth//",
				"!/^@ai-sdk//"
			]
		},
		{
			"description": "Group Better Auth ecosystem non-major updates for manual review",
			"matchUpdateTypes": ["minor", "patch", "pin", "digest"],
			"groupName": "better-auth ecosystem",
			"groupSlug": "better-auth-ecosystem",
			"matchPackageNames": [
				"/^better-auth$/",
				"/^@better-auth//",
				"/^@convex-dev/better-auth$/",
				"/^@mmailaender/convex-better-auth-svelte$/"
			]
		},
		{
			"description": "Disable Better Auth ecosystem major updates until the stack is migrated together",
			"matchUpdateTypes": ["major"],
			"enabled": false,
			"matchPackageNames": [
				"/^better-auth$/",
				"/^@better-auth//",
				"/^@convex-dev/better-auth$/",
				"/^@mmailaender/convex-better-auth-svelte$/"
			]
		},
		{
			"description": "Group AI ecosystem non-major updates for manual review",
			"matchUpdateTypes": ["minor", "patch", "pin", "digest"],
			"groupName": "ai ecosystem",
			"groupSlug": "ai-ecosystem",
			"matchPackageNames": [
				"/^ai$/",
				"/^@ai-sdk//",
				"/^@convex-dev/agent$/",
				"/^@openrouter/ai-sdk-provider$/"
			]
		},
		{
			"description": "Disable AI ecosystem major updates while Convex Agent pins the AI SDK major. It peers on ai ^6 today, so the next major has nowhere to land until the agent follows",
			"matchUpdateTypes": ["major"],
			"enabled": false,
			"matchPackageNames": [
				"/^ai$/",
				"/^@ai-sdk//",
				"/^@convex-dev/agent$/",
				"/^@openrouter/ai-sdk-provider$/"
			]
		},
		{
			"description": "Disable autumn-js major updates until @useautumn/convex ships an autumn-js 1.x compatible release (#666)",
			"matchUpdateTypes": ["major"],
			"enabled": false,
			"matchPackageNames": ["autumn-js"]
		},
		{
			"description": "Disable TypeScript major updates until Svelte tooling supports the native compiler API",
			"matchPackageNames": ["typescript"],
			"matchUpdateTypes": ["major"],
			"enabled": false
		},
		{
			"description": "Group ESLint non-major updates for manual review",
			"matchPackageNames": ["eslint", "@eslint/compat", "@eslint/js"],
			"matchUpdateTypes": ["minor", "patch", "pin", "digest"],
			"groupName": "eslint toolchain",
			"groupSlug": "eslint-toolchain"
		},
		{
			"description": "Disable ESLint major updates until codebase rule fixes are done",
			"matchPackageNames": ["eslint", "@eslint/compat", "@eslint/js"],
			"matchUpdateTypes": ["major"],
			"enabled": false
		},
		{
			"description": "Suppress abandonment warnings for packages we keep intentionally. clsx and husky are stable/feature-complete, date-fns still ships fixes, vaul-svelte is stuck on 1.0.0-next.7 but has no viable Svelte 5 alternative, mode-watcher is dormant (last release 2025-06) but shallowly integrated with no active replacement; on the next Svelte major bump watch for the render_fn error (svecosystem/mode-watcher#161) and fall back to a ~50-line self-roll if it bites.",
			"matchPackageNames": ["clsx", "date-fns", "husky", "mode-watcher", "vaul-svelte"],
			"abandonmentThreshold": "10 years"
		},
		{
			"description": "Keep the pin conversion out of the upgrade groups. They list pin next to minor and patch, so the one-off conversion would join a branch that stays open for weeks, and until that branch merges the ranges it converts are still on main and still invisible to the advisory scan. The two ecosystem families get their own pin branches below rather than this one, for a different reason.",
			"matchUpdateTypes": ["pin"],
			"matchPackageNames": [
				"!/^better-auth$/",
				"!/^@better-auth//",
				"!/^@convex-dev/better-auth$/",
				"!/^@mmailaender/convex-better-auth-svelte$/",
				"!/^ai$/",
				"!/^@ai-sdk//",
				"!/^@convex-dev/agent$/",
				"!/^@openrouter/ai-sdk-provider$/"
			],
			"groupName": "dependency version pinning",
			"groupSlug": "pin-dependencies"
		},
		{
			"description": "Pin the Better Auth family on a branch of its own. Pinning resolves each package to the newest version its own range allows, independently of its siblings, so a family declared with a mix of ~ and ^ splits across a minor boundary and lands on a peer range it no longer satisfies. That needs reviewing as a set. It does not belong in the ecosystem group either: that branch carries ordinary upgrades and can sit open for weeks, which is the same trap as the generic groups.",
			"matchUpdateTypes": ["pin"],
			"matchPackageNames": [
				"/^better-auth$/",
				"/^@better-auth//",
				"/^@convex-dev/better-auth$/",
				"/^@mmailaender/convex-better-auth-svelte$/"
			],
			"groupName": "better-auth version pinning",
			"groupSlug": "pin-better-auth"
		},
		{
			"description": "Pin the AI family on a branch of its own, for the reason above.",
			"matchUpdateTypes": ["pin"],
			"matchPackageNames": [
				"/^ai$/",
				"/^@ai-sdk//",
				"/^@convex-dev/agent$/",
				"/^@openrouter/ai-sdk-provider$/"
			],
			"groupName": "ai version pinning",
			"groupSlug": "pin-ai"
		}
	]
}
