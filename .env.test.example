# E2E Test Environment Variables
# Copy this file to .env.test and set your values
#
# Setup Steps:
# 1. Start dev server: bun run dev
# 2. Start Convex: bunx convex dev
# 3. Set Convex env var: bunx convex env set AUTH_E2E_TEST_SECRET <value>
# 4. Run setup: bun run setup:test-users
# 5. Run tests: bun run test:e2e
#

# Regular E2E Test User Credentials
TEST_USER_EMAIL=test-user@e2e.example.com
TEST_USER_PASSWORD=TestPassword123!
TEST_USER_NAME=E2E Test User

# Admin E2E Test User Credentials
TEST_ADMIN_EMAIL=test-admin@e2e.example.com
TEST_ADMIN_PASSWORD=TestAdminPassword123!
TEST_ADMIN_NAME=E2E Test Admin

# Secret for test mutations (email verification, admin setup, cleanup)
# Must match the value set in Convex backend (via: bunx convex env set AUTH_E2E_TEST_SECRET <value>)
# Generate with: openssl rand -base64 32
AUTH_E2E_TEST_SECRET=your-secure-random-test-secret

# Convex URL (get from `bunx convex dev` output or Convex dashboard)
PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
