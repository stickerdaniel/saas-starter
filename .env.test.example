# E2E Test Environment Variables
# Copy this file to .env.test and set your values
#
# Setup Steps:
# 1. Set Convex env var: bunx convex env set AUTH_E2E_TEST_SECRET <value>
# 2. Run tests: bun run test:e2e
#
# Test users are automatically created with unique emails each run
# and deleted after tests complete. No user credentials needed!

# Secret for test mutations (email verification, admin setup, user deletion)
# Must match the value set in Convex backend (via: bunx convex env set AUTH_E2E_TEST_SECRET <value>)
# Generate with: openssl rand -base64 32
AUTH_E2E_TEST_SECRET=your-secure-random-test-secret

# Convex URL (get from `bunx convex dev` output or Convex dashboard)
PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Site URL for test user signup/signin requests (defaults to http://localhost:5173)
# Only change this if running tests against a different URL
PUBLIC_SITE_URL=http://localhost:5173
