// A _generated directory below the root is ordinary published code (the email
// templates ship one); only the root _generated is codegen the bundler skips.
import type { RegisteredAction } from 'convex/server';

declare const value: RegisteredAction<'internal', Record<string, never>, Promise<null>>;
export const fromNestedGenerated = value;
