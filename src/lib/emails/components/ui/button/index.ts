import Root, {
	buttonVariants,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant
} from './button.svelte';

export {
	Root,
	type ButtonProps as Props,
	buttonVariants,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	//
	Root as Button
};
